/* update both the projects and the tasks
- redraw tasks based on the project selected 
- variable associated with each project to alter the variable that affects
    which tasks to display on the screen, save in the index?
*/
function redraw() {
    
}

/* goes through all the projects and update them */
function redrawProjects(mainProject) {
    
}

/* goes through all the tasks of the project and put them on screen */
function redrawCurrentTasks(project) {

}

